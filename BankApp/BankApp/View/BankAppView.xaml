<Window x:Class="BankApp.View.BankAppView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:my="clr-namespace:BankApp.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="BankAppView">

    <Window.DataContext>
        <my:BankAppViewModel/>
    </Window.DataContext>
    <Grid>
        <Button Command="{Binding Initialize}" Content="Click me!" Margin="10,10,83.6,106.4"/>
        <TextBox Text="{Binding input, UpdateSourceTrigger=PropertyChanged}" Margin="10,169,137.6,60.4"/> 
        <ComboBox ItemsSource="{Binding Path=Accounts}" DisplayMemberPath="AccountID" Margin="10,215,73.6,27.4"/>
        <Button Content="Button" HorizontalAlignment="Left" Margin="176,181,0,0" VerticalAlignment="Top" Width="75"/>

    </Grid>
</Window>
